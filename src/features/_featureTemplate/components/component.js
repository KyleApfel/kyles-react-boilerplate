import React, { PropTypes } from 'react';

const style = {};

const Component = props => {
  return (
    <div>
      { '<COMPONENT_TEMPLATE />' }
    </div>
  );
}

// TODO Add propTypes
Component.propTypes = {};

export default Component;
